#### AI 生成图片（`gen_img_*`）

如果场景大纲的 `mediaGenerations` 中包含图片条目，可以使用对应的生成图片占位符：

- `src` 可以是 `"gen_img_1"`、`"gen_img_2"` 等生成图片 ID
- 幻灯片创建后，这些占位符会替换为实际生成图片
- 使用与原始图片相同的定位规则
- 生成图片的默认宽高比为 16:9
- 未指定其他比例时，按 `height = width / 1.778` 计算高度
- 文字与图片的垂直间距为 25-35px，水平间距为 30-40px
