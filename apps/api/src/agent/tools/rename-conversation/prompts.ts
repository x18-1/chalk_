/** Model-facing usage guidance for rename_current_conversation. */
export const RENAME_CONVERSATION_PROMPT =
  'Use rename_current_conversation only when the user explicitly asks to change the current conversation title. ' +
  'The operation persists user data and always requires approval. Keep the title concise and free of control characters.';
