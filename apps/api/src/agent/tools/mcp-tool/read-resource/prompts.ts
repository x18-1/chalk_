/** Model-facing usage guidance for read_mcp_resource. */
export const READ_MCP_RESOURCE_PROMPT =
  'Use read_mcp_resource to read a bounded text resource exposed by an authorized MCP server. ' +
  'Use the server/resource id returned by MCP discovery and continue with the cursor when needed.';
